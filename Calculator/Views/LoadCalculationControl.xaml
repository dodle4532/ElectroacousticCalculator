<UserControl x:Class="Calculator.Views.LoadCalculationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Calculator.Views"
             mc:Ignorable="d" Height="280" Width="560" >
    <Border BorderBrush="#FF007ACC" 
        BorderThickness="2" 
        CornerRadius="5" 
        Padding="15" 
        Margin="5">

        <GroupBox 
              BorderBrush="#FF007ACC" 
              BorderThickness="1"
              Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Label Content="Выберите наименование рассчета"  VerticalContentAlignment="Center" FontSize="20" HorizontalContentAlignment="Center"  FontWeight="Bold" Margin="0,7,0,7" HorizontalAlignment="Center" Width="368"/>
                <ComboBox x:Name="cbName" HorizontalContentAlignment="Left" ItemsSource="{Binding Calculation}" DisplayMemberPath="Name" Height="NaN"  VerticalContentAlignment="Center" Grid.Row="1" Margin="92,10,92,10"/>
                <Button Content="Загрузить" Height="NaN" FontSize="20" x:Name="btLoad" Click="btLoad_Click" Grid.Row="2" Margin="168,18,168,0" FontWeight="Bold"/>
            </Grid>
        </GroupBox>
    </Border>
</UserControl>
